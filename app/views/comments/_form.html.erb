<%= form_for [event, comment] do |f| %>
  <%= f.label :content, 'Comment' %></br>
  <%= f.text_area :content %></br>

  <%= hidden_field_tag 'comment[employee_id]', session[:user_id] %>
  <%= hidden_field_tag 'comment[event_id]', params[:event_id] %>

  <%= f.submit %>
<% end %>
